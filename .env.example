# Google Analytics 4 Measurement ID (Admin → Data Streams → your web stream)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-B9WML6K6T1

# Google Search Console HTML tag verification value (Settings → Ownership verification → HTML tag)
# Paste only the `content` attribute value, e.g. from <meta name="google-site-verification" content="abc123" />
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
